# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
source 'https://github.com/CocoaPods/Specs.git'

target 'OMHSerializer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OMHSerializer
  pod 'Granola', '~> 0.4'
  pod 'Specta', '~> 1.0'
  pod 'Expecta', '~> 1.0'
  pod 'VVJSONSchemaValidation', '~> 1.3'
  pod 'ObjectiveSugar', '~> 1.0'
  pod 'CorePlot', '~> 2.3'

  target 'OMHSerializerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OMHSerializerUITests' do
    # Pods for testing
  end

end
